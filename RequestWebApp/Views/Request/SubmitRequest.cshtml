@{
    ViewBag.Title = "Submit Request";
}

<h2>Submit Request</h2>

<form method="post" action="@Url.Action("SubmitRequest", "Request")">
    <button type="submit" class="btn btn-primary">Submit Request</button>
</form>
